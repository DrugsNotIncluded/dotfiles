#include <stdio.h>
#include <sys/socket.h>


int main (int argc, char *argv[]) {
	struct sockaddr
	int sock_descriptor;
	sock_descriptor = socket(AF_INET, SOCK_STREAM, 0);

	if (sock_descriptor == -1) {puts("Socket creation failed.");}

}
